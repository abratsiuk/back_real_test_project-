# Получите стандартный .gitignore для .NET
curl -o .gitignore https://raw.githubusercontent.com/dotnet/core/main/.gitignore

# Всегда используйте .gitignore для .NET проектов

Не коммитьте папки .vs/, bin/, obj/

# Закрывайте Visual Studio перед Git операциями